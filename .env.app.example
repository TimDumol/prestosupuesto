# Public OAuth/application identifiers. These are embedded in the APK and are
# not secrets. The OAuth client is still restricted by package name/signature,
# user consent, the Apps Script executable, and its two-user allowlist.
EXPO_PUBLIC_GOOGLE_WEB_CLIENT_ID=
EXPO_PUBLIC_APPS_SCRIPT_DEPLOYMENT_ID=
